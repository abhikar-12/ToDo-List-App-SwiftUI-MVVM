# ToDo-List-App-SwiftUI-MVVM
A To-Do List app built using SwiftUI and MVVM architecture. Features include adding tasks via sheet, deleting with swipe gesture, navigation to detail view, and state management using @StateObject, @ObservedObject, and @Published.
